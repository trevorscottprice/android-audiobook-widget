<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <!-- Open ring with a gap at the upper left. Radius is close to the
         viewport edge so the ring reads the same size as the play circle. -->
    <path
        android:pathData="M12,2 A10,10 0 1 1 4.93,4.93"
        android:strokeColor="@color/widget_text"
        android:strokeWidth="2"
        android:strokeLineCap="round" />
    <!-- Arrow head pointing counter-clockwise. -->
    <path
        android:pathData="M12,0 L12,4.2 L8.8,2.1 Z"
        android:fillColor="@color/widget_text" />
</vector>
